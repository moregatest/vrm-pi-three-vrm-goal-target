{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the main VRM Pet window",
  "windows": ["main"],
  "permissions": ["core:default"]
}
